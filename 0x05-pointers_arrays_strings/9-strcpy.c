#include "main.h"

/**
* _strcpy -  prints n elements of an array
*
* @dest: char pointer 1
* @src: char  pointer 2
*
* Return: dest
*/

char *_strcpy(char *dest, char *src);
{
	int i = 0;

	while (scr[i])
	{
		dest[i] = scr[i];
		i++
	}
	return (dest);
}
