#include "main.h"
/**
 *print_square - Entry point
 *
 *Description: prints a square
 *
 * @size: number
 *
 * Return: result
*/

void print_square(int size)
{
	int i, l;

	for (i = 0; i < size; i++)
	{
		if (n <= 0)
			break;
		for (l = 0; l < size; l++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}
}
